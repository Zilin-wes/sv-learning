# 02数组
## 1.声明
```systemverilog
byte array_0 [8]; //8个独立的成员变量 byte后不能加[] 即byte [3:0] array_2 [0:7];是错的 因为byte有固定8bit无法修改
byte array_1[0:7][0:3]; //8x4个独立的

bit [3:0] array_2 [0:7][0:3]; // packed array unpacked 8x4个独立的成员变量,成员变量是4个bit
bit [3:0][1:0] array_3 [0:7][0:3]; //次高级 最低级 array 最高级 8x4个独立的成员变量,成员变量是4个packed的2个bit

logic [7:0] array_4 [0:3];  //四值变量默认x 二值默认0

int ids;

initial begin
//1.赋值
  array_0[0]= 8'h55;  //单个赋值:<位宽>'<进制><数值>  b二进制 h十六进制 o八进制 d十进制
  array_1[2]='{8'h00, 8'h01, 8'h02, 8'h03};  //成组赋值'{}
// unpacked 不能赋值给packed

//2.元素个数
  ids= $size(array_3);  //返回当前维度的元素个数8
  #10;
  ids= $size(array_3[1]);  //4
  #10;
  ids= $size(array_3[1][0]);  //4
  #10;
  ids= $size(array_3[1][0][0]);  //2
  #10;
  ids= $size(array_3[1][0][0][0]);  //0

//3.循环(类似for)  
  foreach(array_0[i]) begin
      $display("array_0[%0d]=%0h.", i, array_0[i]);
  //output: array_0[0]=85. 从小到大
  //array_0[1]=0.
  //array_0[2]=0. ...

  foreach(array_2[i, j]) begin
      $display("array_2[%0d][%0d]=%0h.", i,j, array_2[i][j]);  //foreach不能遍历packed
 //output: array_2[0][0]=0.
  //array_2[0][1]=0.
  //array_2[0][2]=0. ...
  
end
```
<img width="989" height="583" alt="image" src="https://github.com/user-attachments/assets/501dbbad-f557-4333-8d2a-29d488123493" />
<img width="947" height="458" alt="image" src="https://github.com/user-attachments/assets/3ab7f4af-1a10-43d0-b3a9-2dac0cf06b5b" />

## 2.动态数组

```systemverilog

int dyn[], d2[];
byte d3[]='{-1, 0, 1}; //声明三个动态数组 

innitial begin
  dyn=new[5]; //操作前必须用new命令分配数组大小 此处分配5个
  foreach(dyn[i])
      dyn[i]=i*2;
  d2=dyn;
  d2[0]=6;
  $display(dyn[0], d2[0]);

  dyn=new[10](dyn);//分配10个cell 0-4copy原来的dyn
  foreach(dyn[i])
      $write("%0d", dyn[i]);

  dyn.delete(); //删除内存分配
end
```

## 3.队列

```systemverilog
int j=2, q2[$]={3,4}, q3[$];  //{}没有'
int q4[$];  //$表示索引的最大值

initial begin
    #300;
    q3 = {9,10};
    q2.insert(0, j);      //{2,3,4} 在q2[0]处插入j
    q2.push_front(1);     //{1,2,3,4}
    q2.push_back(5);      //{1,2,3,4,5}
    j=q2.pop_front();     //j=1, {2,3,4,5}
    $display("j=%0d", j);
    foreach(q2[i])
        $write("%0d ", q2[i]);
    $display();
    
    q2 = {q2, q3};        //{2,3,4,5,9,10}
    q4 = q2[2:$-1];       //{4,5,9} $表示索引的最大值
    q4 = q2[0:1];         //{2,3}
    $display("size of q4 is: %0d.", $size(q4));
    q2.delete(2);         //{2,3,5,9,10}
    q2.delete();          //q2 is empty
end
```

## 4.关联数组(哈希数组)
### 数组：不能做到一一对应 name[0]=LIsa score[0]=89
### 关联数组：可以一一对应，更加方便 score["Lisa"]=89 省内存，不用像数组一样第129个要分配129个空间
### first next delete exists 
```systemverilog
shortint aa[int];    // 关联数组名[数据类型] int aa_s[string];
int      idx = 8;    //索引初值8
logic    f;

initial begin
    #400;
    $display("---- associate array test ----");
    repeat(4) begin
        aa[idx] = idx;    //aa[8]=8; aa[16]=16; aa[32]=32; aa[64]=64
        idx = idx << 1;    //左移一位，相当于x2 idx=128
    end
    $display("size of aa is %0d.", $size(aa));    //size of aa is 4.
    foreach(aa[i])
        $write("%0d ", aa[i]);     // 8 16 32 64
    $display("");    //输出回车

    if(aa.first(idx)) begin    //最小的key（idx），8，非0
        $write("index of aa is: %0d ", idx);    //index of aa is: 8
        while(aa.next(idx)) begin    //循环；下一个key（idx），16，非0
            $write("%0d ", idx);      // 16
        end      //第三次循环输出32 第四次循环输出64 没有下一个key， 记为0，循环停止 进入else
        $display("");
    end else begin
        $display("aa is empty!");
    end

    f=aa.exists(idx);    //检查当前索引元素是否存在，存在f=1，不存在f=0

    aa.delete(idx);    //idx=64 删除当前索引的角标元素 [8 16 32]
    $display("size of aa is %0d.", $size(aa));      //size of aa is 3.
    aa.delete();      //删除aa
    $display("size of aa is %0d.", $size(aa));    //size of aa is 0.
end

```


## 5.数组缩减
### 1.sum() 所有元素求和
#### 所有数组都可用 队列、关联数组。。。
```systemverilog
byte score[4] = '{25, 50, 100, 120};
int  sum;

initial begin
    #500;
    $display("---- array method test ----");
    $display("1: sum of score is %0d.", score.sum());    //39    score是byte，8位有符号数，-128~127，取后八位：295-256=39
    $display("2: sum of score is %0d.", (score.sum() + 32'd0));    //39    +32'd0提升到32位
    sum = score.sum();
    $display("3: sum of score is %0d.", sum);    //39
    $display("4: sum of score is %0d.", score.sum() with(int'(item)));    //295      int'(item)把score里每个元素转化为int类型 with表示先执行with后的语句
end
```
### 2.product(), and(), or90, xor()

### 3.数组定位 排序

| 方法 | 作用 | 是否修改原数组 | 返回值 |
|------|------|:------------:|:------:|
| `min()` | 找最小值 | 否 | 单个值 |
| `max()` | 找最大值 | 否 | 单个值 |
| `sum()` | 求和 | 否 | 单个值 |
| `reverse()` | 原地反转数组 | **是** | 无 |
| `sort()` | 原地升序排序 | **是** | 无 |
| `rsort()` | 原地降序排序 | **是** | 无 |
| `shuffle()` | 原地随机打乱 | **是** | 无 |
| `unique()` | 剔除重复元素 | 否 | **新数组** |
| `find() with` | 返回符合条件的所有元素 | 否 | **新数组** |

```systemverilog
byte score[4] = '{100, 25, 25, 120};
byte score_s[];
int  sum, max, min;

initial begin
    #510;
    min = byte'(score.min());            //25
    max = byte'(score.max());            //120
    $display("min=%0d, max=%0d.", min, max);
    score.reverse();                     //120,25,25,100
    print_byte_array(score);
    score.sort();                        //25,25,100,120
    print_byte_array(score);
    score_s = score.unique();            //25,100,120
    print_byte_array(score_s);
    score_tmp = score.find with (item > 25);   //100,120
    print_byte_array(score_tmp);         //100,120
end
```
